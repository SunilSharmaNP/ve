# 29. heroku.yml - Heroku container configuration
# Enhanced VideoCompress Bot Heroku Configuration v2.0
build:
  docker:
    web: Dockerfile
    
setup:
  config:
    PYTHON_VERSION: 3.10.12
    
run:
  web: python -m bot
